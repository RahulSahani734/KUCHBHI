name: My First Workflow

on:
  workflow_dispatch:
jobs:
  jobe1:
    runs-on: ubuntu-latest
    steps: 
      - name: hello-job
        run: echo " sdfsdf" 
    
    steps:
      - name: Print Hello
        run: echo "Hello! GitHub Action"
    
  
